---
description: claw-family 架构约束；所有代码改动必须符合本规则
alwaysApply: true
---

# claw-family 架构约束

所有代码改动必须遵守本规则。详见 **docs/prd-bootstrap.md**。

## 项目定位

- **claw-family**：OpenClaw 的封装，提供飞书机器人 + 阿布人设（2 岁小女孩，中文）。
- 用户：jojo（妈妈，金融）、slashhuang（爸爸，程序员）。

## 目录与职责（不得破坏）

| 目录/文件 | 职责 | 代码改动注意 |
|-----------|------|----------------|
| `config/` | OpenClaw 配置 | 只改 `openclaw.partial.json`、`README.md`；**不要改** `openClawRuntime/`（自动生成） |
| `scripts/` | 启动、环境、runtime 生成 | 主入口 `start-openclaw.sh --env dev/prod`；生产入口 `bootstrap.sh` |
| `skills/` | 能力插件 | 新 skill 放入 `skills/<name>/` 并含 `SKILL.md` 即可，由目录加载 |
| `workspace-defaults/` | 默认 workspace 内容 | 改人设/工作流只改此处；**不要改** `openClawRuntime/.workspace/`（gitignored）。启动时脚本用 defaults 的 BOOT.md 覆盖 workspace |
| `docs/` | PRD、说明文档 | 功能/结构类变更需先有 PRD |

## 配置分层（必须遵守）

1. **主配置**：`config/openclaw.partial.json`（模型、gateway）；飞书由 `bot.dev.json` / `bot.prod.json` 提供。
2. **运行时配置**：`openClawRuntime/openclaw.generated.json` — **仅由脚本生成**（注入 skills、workspace 等），禁止手改或提交。

合并逻辑见 `config/README.md`；生成由 `scripts/ensure-openclaw-runtime.sh` 负责。

## 工作区与 gitignore

- 编辑「工作区里阿布用的文件」时，只改 **`workspace-defaults/`** 下对应文件。
- 禁止修改 **`openClawRuntime/`**、`.env` 等 `.gitignore` 内路径（详见规则「不要改到 gitignore 里的路径」）。

## 开发流程

- **PRD 先行**：功能/结构变更前，在 `docs/` 下应有对应 PRD。
- **分支**：功能开发用 `feat/<name>`。
- **Skills**：新 skill 放在 `skills/<name>/`，含 `SKILL.md`，由目录加载。

## 启动与环境

- **本地开发**：`./scripts/start-openclaw.sh --env dev`（仅 test 飞书账号）。
- **生产**：`./bootstrap.sh`（代码同步 + PM2；jojo、slashhuang）。
- 环境由 **`--env`** 指定，不用 `.env`。

## 小结

- **可改**：`config/openclaw.partial.json`、`skills/`、`scripts/`、`workspace-defaults/`、`docs/`。
- **不可改**：`openClawRuntime/`、`.env` 等 gitignore 路径。
- **结构性变更**：先 PRD，再改代码。
